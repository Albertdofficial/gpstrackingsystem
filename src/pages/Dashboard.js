import React from "react";

import './dashboard.css'

const Dashboard = () => {
  return (
    <div className="dashboard" >
      Driver Dashboard
      <p>Driver dashboard is accessible only to users authenticated as drivers</p>
    </div>
  );
};

export default Dashboard;
